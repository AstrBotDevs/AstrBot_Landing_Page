/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "trendshift.io",
      "contrib.rocks",
      "registry.npmmirror.com",
    ],
  },
};

export default nextConfig;


